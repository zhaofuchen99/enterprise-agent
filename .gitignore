# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 工具缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 环境与密钥：任何情况下不得提交
.env
.env.*
!.env.example

# 设计文档与个人材料：只留在本地，不进版本库。
# 这些是**设计意图的记录**，不是可交付物；且仓库是公开的，其中含个人准备材料。
# 注意 CLAUDE.md 在仓库根目录、不属于此目录，仍然提交。
docs/

# 本地数据（容器卷、语料、原始文档）
data/
.minio/
*.sqlite3

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# 操作系统
.DS_Store
Thumbs.db
